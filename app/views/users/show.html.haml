- provide(:title, "#{@user.email}")
%h2
  User: #{@user.email}
%h2 Balance: #{'%.2f' % @user.balance}
%p 
- if @user.bets.any?
  -#
    Тут выбор даты/календарь/пагинация
    %h4 Bets:
      = render @user.bets