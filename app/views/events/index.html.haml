- provide(:title, 'All events')
= render "search"

%h2
  All events:
  - if can? :create, @event
    = link_to "Add event", new_event_path, remote: true, class: 'btn btn-primary'
= paginate @events
= render "index"

#event-modal.modal.hide.fade